# Changelog

## 0.3.2 — 2026-06-02

- Extension ID `amirmkazemi.bidi-forge`; display name **BIDI · Forge**; VSIX file `BIDI-Forge-0.3.2.vsix`.
- Open VSX listing: full Resources (homepage, repository, bugs), tags, overview README, gallery banner.

## 0.3.1 — 2026-06-02

- Localized command and settings titles (`package.nls.json`, `package.nls.fa.json`).
- `rtlFixer.fixOnPaste` setting for automatic fix on paste.
- RTL UI troubleshooting doc bundled (`TROUBLESHOOTING-RTL-UI.md`).
- Expanded `rtl-ui.css` for VS Code chat and AI panel inputs.
- Core engine bundled via esbuild workspace aliases (standalone VSIX).

## 0.3.0

- BiDi-safe notifications, settings webview, status bar, scoped RTL UI CSS (Custom CSS loader).
